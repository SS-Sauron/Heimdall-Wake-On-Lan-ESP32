idf_component_register(
    SRCS
        "wifi_sta.c"
    INCLUDE_DIRS
        "."
    REQUIRES
        esp_wifi
        esp_netif
        esp_event
        storage
        identity
    PRIV_REQUIRES
        esp_timer
        espressif__mdns
        lwip
        status_led
)
